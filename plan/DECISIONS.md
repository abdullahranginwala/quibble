# Decisions log (append-only)

- 2026-07-07 M0: `go.mod` directive is `1.25`, not `1.24` — `go-internal@v1.15` (testscript) requires 1.25; toolchain installed is 1.26.4. Conventions file said 1.24; bump was forced by the allowed dep set.
- 2026-07-07 M0: `config.Default()` authors are `human`/`agent` (generic); `quibble init` keeps them — users personalize in config. Validate() additionally requires human ≠ agent since the resolve policy gate depends on distinguishing them.
- 2026-07-07 M0: glob matcher also skips all dot-directories (not just `.quibble`) during doc walks — hidden dirs as doc sources are out of scope.
- 2026-07-07 M1: `NormalizeBlocks` signature is `func(src []byte) (text string, blocks []Block)` — the spec exports the name but leaves the signature open; this shape is the single source of truth for both `Doc.Text` and `Doc.Blocks`, so `RenderDoc` and `pkg/comment` consume the identical function. Frozen by golden `testdata/golden/normalize.json`.
- 2026-07-07 M1: paper theme assets live at `pkg/render/themes/paper/` (embedded via `go:embed themes`), not `web/themes/`. `go:embed` patterns cannot cross package boundaries (no `..`), and the M1 hard constraint forbids touching files outside `pkg/render`. Assets are trivially relocatable to a `web/` package later if a shared frontend tree is wanted.
- 2026-07-07 M1: block enumeration — a list contributes one block per *direct* list item; nested sub-lists fold their text into the containing item's block (not enumerated separately). Blocks with empty normalized text (e.g. thematic breaks, image-only paragraphs with empty alt) are excluded from `Doc.Text`/`Doc.Blocks` and receive no `data-qbl`. Spec was silent on nesting and empties; pinned by the kitchen-sink goldens.
- 2026-07-07 M1: chroma styles are `github` (light) / `github-dark` (dark), emitted as classes and scoped under `[data-qbl-scheme="dark"]` plus a `prefers-color-scheme: dark` media query. Custom `data-qbl` attributes ride goldmark's default block renderers (goldmark renders any `data-*` attribute regardless of its per-node filter), so only heading + code-block rendering is overridden.
- 2026-07-07 M1: renderer uses `html.WithUnsafe()` so author-authored raw HTML in trusted local markdown passes through (docs are the user's own repo files, per DESIGN §13's no-mutation / local-first stance). Revisit if untrusted markdown is ever rendered.
- 2026-07-07 M2: `pkg/comment` defines its own minimal `Heading{Level,Text,Anchor}` and `Block{ID,Text,Start,End}` instead of importing `render.Heading`/`render.Block` (the spec's `Sectionize` signature named them). Keeps `pkg/comment` self-contained and buildable while `pkg/render` is developed in parallel; the field sets mirror render's so the two reconcile with a type alias later if desired. Tests vendor render's frozen normalization rule (03-m1-render.md step 4: collapse intra-block whitespace runs to one space + trim, join blocks with "\n") in a local helper rather than importing `render.NormalizeBlocks`.
- 2026-07-07 M2: `Marshal` hand-writes the frozen frontmatter (fixed key order; anchor exact/prefix/suffix always double-quoted; heading as a flow sequence, omitted when empty; timestamps as plain RFC3339 scalars; resolved_by/resolved_at emitted only when set) for byte-exact determinism, while `ParseThread` decodes frontmatter with yaml.v3 for whitespace tolerance. Top-level id/doc/status/author are emitted plain when they match a safe pattern, else double-quoted — the frozen example values are all plain.
- 2026-07-07 M2: reply-marker rule is line-start + a *fully RFC3339-shaped* timestamp (not the looser `time=\S+`). Consequence/known limitation (test row 3): a body or reply-body line that is itself a fully valid `<!-- reply author=… time=<RFC3339> -->` at line start will be re-read as a reply on a Marshal→Parse round-trip. A body line resembling a marker but carrying a non-RFC3339 time stays in the body and round-trips cleanly (regression: fuzz-found input 214c8ae1, `TestRegression_BogusTimeMarkerStaysBody`). The `FuzzParseThread` round-trip invariant excludes the fully-valid-marker-in-body case; the no-panic contract holds unconditionally.
- 2026-07-07 M2: fuzzy `scanWindows` adds two result-preserving speedups the spec's perf bar demands. (1) A rolling character-multiset (bag) L1 distance is maintained incrementally over the length-L window; bag distance ≤ edit distance, so windows whose `bag/2` exceeds the working band are skipped before any DP (a sound lower-bound prefilter, gated on the 1.0× window and applied to all three width variants — a heuristic for the 0.8×/1.2× variants that never produces a false match and, given overlapping windows + small step, does not change outcomes on any specced case). (2) The banded-Levenshtein band shrinks to `maxLen·(1−bestScore)` as the best score improves, so once a strong match is seen, dissimilar windows early-exit almost immediately. Measured 200 KB fuzzy `Resolve`: ~54 ms (design bar < 100 ms), ~304 ms under `-race` (assert < 500 ms).
- 2026-07-07 M3: slug-collision rule (test row 21) — `slugFor` is the pure function from conventions §Naming (drop `path.Ext`, replace `/` with `--`) and is deliberately non-injective: a name already containing `--` can collide with a nested path (`a--b.md` and `a/b.md` both slug to `a--b`). Accepted as-is: the two docs' threads share one directory but stay distinct by ID, and the frontmatter `doc:` field (not the directory name) is authoritative for `List` filtering, so no behavior is ambiguous. Same input → same slug, forever; no de-dup suffixing.
- 2026-07-07 M3: `FSStore.Warnings()` reflects only the most recent `List` call (reset at the start of each scan), so `doctor` always sees current state rather than an accumulating log. "List errors only if nothing readable" is interpreted as: ≥1 file present and 0 parseable → error; an empty or absent comments dir → empty slice, nil error.
- 2026-07-07 M3: same-status `SetStatus` no-op returns success without touching disk (no rewrite, actor ignored); `actor` is only recorded on transitions *to* resolved. Per-ID mutexes live in a map that grows monotonically per process — fine for CLI/server lifetimes; cross-process safety is explicitly git's job (documented on FSStore).
- 2026-07-07 M4: the render→comment type bridge (render.Doc.Outline/Blocks/Text → comment.Heading/Block + Sectionize) lives in `internal/cli/helpers.go` (`docAnchorContext`), not a separate `internal/bridge` package. It is a ~10-line field copy used only by the CLI; a dedicated package would be more ceremony than value. Anchoring renders each doc with the plain `render.Paper()` renderer (no theme overrides) since normalization is theme-independent — this keeps `add`/`doctor`/`repin` from failing on a config with bad override tokens (doctor reports those separately).
- 2026-07-07 M4: `build` runs two render passes — a per-doc pass to isolate failures (a bad/unreadable doc is listed, not fatal) and a `RenderDir` over a `testing/fstest.MapFS` of the good sources to produce the authoritative site (index + pages + assets). `testing/fstest` does not import `testing` (verified via `go list -deps`), so using it in non-test code adds no test-only weight. Redundant work is negligible for a CLI and buys true partial-failure semantics for both read and render errors.
- 2026-07-07 M4: anchor rewrites for `doctor --fix` and `comments repin` are done by the CLI writing the thread file directly (atomic temp+rename via `rewriteAnchor`/`findThreadFile` in internal/cli), because `store.CommentStore` intentionally has no anchor-update method and pkg/* is out of M4 scope. `Thread.Marshal` (public, frozen format) is the on-disk writer, so the file stays byte-format-identical to store writes.
- 2026-07-07 M4: `doctor` exit precedence is 3 > 2 > 0; a configured theme-override that names an unknown `--qbl-*` token is classed with orphan/corrupt as exit 3 (validated by handing the overrides to `render.New`, which owns the token list — no need to export `requiredTokens`). Zero-config `build` synthesizes `{docs:["**/*.md"], theme:paper}` inline rather than reusing `config.Default()` (whose docs globs differ), per the milestone's stated zero-config defaults.
- 2026-07-07 M4: `comments add` prints only the new thread id to stdout (or a full JSON object under `--json`) so scripts/agents can consume it directly. The e2e harness (cli_test.go) registers a `readfirst VAR FILE REGEXP` testscript command that binds a captured id to an env var, since testscript has no built-in stdout→variable capture; scripts mark a git work tree with a bare `mkdir .git` (no git binary needed — `init` only stats for `.git`).
- 2026-07-07 M5: the render→comment bridge (M4 kept it inline in internal/cli/helpers.go) moved to a new shared package `internal/docbridge`. M5's server needs the same Text/Blocks/Sections/Page bundle the CLI derives, so a one-owner field-copy is now justified over duplication. `docbridge.Render(r, src, relPath)` returns a `*Rendered` carrying the HTML Page plus the comment-typed model; `docAnchorContext` in the CLI now calls it (imports updated). Supersedes the M4 "bridge lives in internal/cli" decision.
- 2026-07-07 M5: embed arrangement — the frontend (comments.js, anchor-render.js, ui.css) lives in a top-level Go package `web` with its own `//go:embed`, and `internal/server` imports `web.Assets`. `go:embed` cannot cross package boundaries (no `..`), so the assets must be embedded from a package that sits at or above them; a `web` package is the simplest and matches DESIGN §11's layout. The renderer's own theme/highlight CSS (tokens/theme/chroma/quibble) is captured once at server start via `RenderDir(fstest.MapFS{})` and served from the in-memory asset map.
- 2026-07-07 M5: asset URL resolution without changing the frozen `assetPrefix` (`qbl/`, relative — build goldens depend on it). Doc pages served at `/d/{slug}` resolve the layout's relative `qbl/*` refs to `/d/qbl/*`; the server therefore mounts the combined asset handler at BOTH `/qbl/` and `/d/qbl/` (index page + JS's absolute `/qbl/ui.css` use the former; doc-page CSS + comments.js use the latter). The layout template is left UNCHANGED — an earlier attempt to make comments.js absolute broke pkg/render's "self-contained static site" test (row 9), and the dual mount removes the need for any layout edit while keeping `build` output relative and harmless.
- 2026-07-07 M5: docVersion etag = first 12 hex of sha256(normalized doc text). It rides GET /api/docs/{slug}/comments; POST /api/comments echoes it back and a mismatch is 409 (optimistic concurrency, no timestamps). Server-side quote re-verification: the POST body carries the client's `quote`; the server slices its own normalized text at `blockStart+offset` and 400s unless it equals `quote` — client rune offsets are never trusted (rows 4/5).
- 2026-07-07 M5: fsnotify watches the project root, each doc dir, and the `.quibble/comments` tree (pre-created with MkdirAll so the first-ever comment's slug-dir CREATE is caught — row 11); newly created dirs are added to the watcher on the fly. Events are classified by path (under comments/ → comments-changed{slug from dir name}; matching a doc glob → doc-changed{slug}) and debounced 150 ms so a burst (editor save+chmod, store temp+rename, two rapid saves) collapses to one SSE event per slug (row 12). Rune offsets are the shared coordinate space end to end; the JS char-map counts Unicode code points (not UTF-16 units) so astral chars stay aligned with Go's rune offsets. Whitespace normalization is replayed in JS to match pkg/render.normalizeText — single-block selection only in v0.1 (cross-block selections offer no comment button).
- 2026-07-07 M6: skill lives at repo-root `skill/` as a tiny Go package (`skill.Content` via go:embed) — single source of truth, no template copy to drift; `internal/cli` imports it for `init --claude`. M4's `init.txtar` placeholder assertion updated to check real contract text ("Never resolve").
- 2026-07-07 M6: `init --claude` on an already-initialized repo still installs the skill (idempotent add-on path); without `.claude/` it skips with a message rather than creating the dir uninvited.
- 2026-07-07 M6: dogfood threads seeded on DESIGN.md: qbl-t6yf5c (slug non-injectivity — document in §3 vs doctor collision detection), qbl-gnw3hq (reply-marker-in-body limitation; ADDRESSED by documenting in §4, RESOLVED by abdullah — lives in _resolved/), qbl-aujm4a (v0.2 theme priority: ink vs terminal). Spec's row-1 golden implemented as a section-presence test rather than a byte golden — the contract text will evolve; its required sections are the invariant.
- 2026-07-07 v0.1 acceptance passed — executed by the orchestrating agent per plan/10-acceptance.md. Sections A (gate, install), B (fresh-project loop: init idempotence, offline build, serve+comment via UI API incl. 409/400 guards, agent loop with live SSE, resolve policy exit 3, _resolved/ move, doctor heal exit 2→0, orphan repin exit 3→0, readable git history), C (dogfood serve; fresh-agent test: an unbriefed agent discovered .quibble/AGENTS.md, addressed both open threads with real DESIGN.md edits, did not resolve), D (zero-config build, corrupt-file resilience, kill -9 leaves no .tmp, Hindi+emoji anchors exact). Step B5's browser-visual half and the m5-manual.md checklist remain for human eyes; every scripted/API-verifiable behavior passed.
