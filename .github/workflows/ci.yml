name: ci
on:
  push:
    branches: [main]
  pull_request:

jobs:
  gate:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
      - name: gofmt
        # `(! grep .)` breaks on macOS bash 3.2 (negation bug) — keep this portable.
        run: |
          out=$(gofmt -l .)
          if [ -n "$out" ]; then printf '%s\n' "$out" "gofmt: files need formatting"; exit 1; fi
      - name: vet
        run: go vet ./...
      - name: build
        run: go build ./...
      - name: test
        run: go test ./... -race
      - name: browser e2e (must not skip)
        env:
          QBL_REQUIRE_BROWSER: "1"
        run: go test ./internal/server -race -count=1 -run TestBrowserSelectCommentFlow -v
